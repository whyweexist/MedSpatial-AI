import { Canvas } from "@react-three/fiber"; import { OrbitControls, Environment, Center } from "@react-three/drei"; import { useRef, useEffect } from "react"; import BrainModel from "./BrainModel"; export default function DetailSection() { const scrollY = useRef(0); useEffect(() => { const handleScroll = () => { scrollY.current = window.scrollY; }; window.addEventListener("scroll", handleScroll); return () => window.removeEventListener("scroll", handleScroll); }, []); return (<div className="w-full min-h-screen bg-[#f5f5f5] flex flex-col items-center justify-center px-24 py-12"> <div className="grid grid-cols-3 gap-24 w-full max-w-7xl font-syne"> {/* Left Section */} <div className="flex flex-col"> <h1 className="text-[64px] font-semibold leading-[1.1] tracking-tight text-black"> We Are <br /> Detail Oriented </h1> {/* 3D Model */} </div> {/* Middle Text */} <div className="text-gray-700 text-[18px] leading-[1.8]"> We convert medical scans into interactive 3D visuals, making complex anatomy easier to explore and understand. </div> {/* Right Text */} <div className="text-gray-700 text-[18px] leading-[1.8]"> Powered by AI, our platform analyzes diseases and provides clear insights and preventive guidance for better healthcare decisions. </div> </div> <div className="mt-12 w-150 h-100 bg-"> <Canvas camera={{ position: [0, 0, 5], fov: 50 }}> {/* Lighting */} <ambientLight intensity={1} /> <directionalLight position={[5, 5, 5]} intensity={2} /> {/* Environment (realistic lighting) */} <Environment preset="studio" /> {/* Centered Model */} <Center> <BrainModel scrollY={scrollY} /> </Center> {/* Controls (optional) */} <OrbitControls enableZoom={false} /> </Canvas> </div> </div>); }