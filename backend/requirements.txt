# MedSpatial AI — Development Backend Dependencies
# =================================================
# Full dependency set for local development and training.
# Includes PyTorch (CPU wheel here; swap index URL for CUDA build).
#
# Install:
#   pip install torch==2.2.0+cpu --index-url https://download.pytorch.org/whl/cpu
#   pip install -r requirements.txt

# ── Web framework ─────────────────────────────────────────────────────────────
fastapi>=0.110.0
uvicorn[standard]==0.27.0
starlette==0.36.3
python-multipart==0.0.6
websockets==12.0
h11==0.14.0
httptools==0.6.1
anyio==4.3.0

# ── Data validation ───────────────────────────────────────────────────────────
pydantic==2.5.3
pydantic-settings==2.1.0

# ── Database ──────────────────────────────────────────────────────────────────
sqlalchemy==2.0.25
aiosqlite==0.19.0

# ── AI / ML — PyTorch stack ───────────────────────────────────────────────────
# Install torch FIRST with the appropriate index URL for your hardware:
#   CPU:  pip install torch==2.2.0+cpu --index-url https://download.pytorch.org/whl/cpu
#   CUDA: pip install torch==2.2.0 --index-url https://download.pytorch.org/whl/cu121
torch==2.2.0
einops==0.7.0

# ── ONNX export & validation (dev / CI only, not needed at runtime) ───────────
onnxruntime==1.17.0
onnx==1.16.0
# onnxscript and onnxconverter-common are optional; install manually when exporting:
#   pip install onnxscript==0.1.0 onnxconverter-common==1.13.0

# ── NLP / embeddings ──────────────────────────────────────────────────────────
# transformers and sentence-transformers pull in torch as a dependency.
# They are used by chat_service.py (MedicalQAModel) and medical_qa.py.
transformers==4.37.2
tokenizers==0.15.1
sentence-transformers==2.3.1
huggingface-hub==0.21.0

# ── Medical imaging ───────────────────────────────────────────────────────────
pydicom==2.4.4
pylibjpeg==2.0.0
pylibjpeg-libjpeg==2.0.0
scikit-image==0.22.0
Pillow==10.2.0

# ── 3-D mesh generation ───────────────────────────────────────────────────────
trimesh==4.1.3
# rtree is an optional trimesh dep for spatial queries; include for full feature set
rtree==1.2.0

# ── Scientific computing ──────────────────────────────────────────────────────
numpy==1.26.3
scipy==1.12.0

# ── Report generation ─────────────────────────────────────────────────────────
# report_service._render_slices_to_images() imports matplotlib at call time.
reportlab==4.1.0
python-docx==1.1.0
matplotlib==3.8.2

# ── File I/O ──────────────────────────────────────────────────────────────────
aiofiles==23.2.1

# ── Logging ───────────────────────────────────────────────────────────────────
loguru==0.7.2

# ── Utilities ─────────────────────────────────────────────────────────────────
psutil==5.9.8
tqdm==4.66.1
httpx==0.26.0

# ── Dev / test only ───────────────────────────────────────────────────────────
pytest==7.4.4
pytest-asyncio==0.23.3
