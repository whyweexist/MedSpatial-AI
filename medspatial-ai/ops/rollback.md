# Rollback

Rollback application images and model registry entries independently. Preserve
AWM files, audit logs, uploaded studies, and database backups. Never downgrade
an AWM schema without an explicit reversible migration.
